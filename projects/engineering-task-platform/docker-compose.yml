services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: engineering_task_platform
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - task-platform-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d engineering_task_platform"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  task-platform-postgres:
